console.log('start work');

const enableValidation = () => {
  // find all forms
  // set event lesteners each form
  // prevent page reload on form submit
  // find all inputs
  // find submit button
  // add listeners for each input
  //// check each input is valid // if valid, hide error else show error
  //// toggle button state // if form valid enable button else disable
  // set initial button state
};
